from setuptools import setup, find_packages

setup(
    name = "defer-defaults",
    author = "Peter OConnor, JonathPeter OConnor, Jonathan Fine",
    version = 0,
    dependency_links = (),
    install_requires = [''],
    scripts = [],
    packages=find_packages(),
    )
